{
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "description", "language", "needsReview"],
  "properties": {
    "title": { "type": "string" },
    "description": { "type": ["string", "null"] },
    "language": { "type": "string", "enum": ["fi", "en", "mixed", "und"] },
    "needsReview": { "type": "boolean" }
  }
}
