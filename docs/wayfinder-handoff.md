# Wayfinder recovery and handoff

Recovered 2026-09-12 from the earlier Codex session `01a09576-4b82-7a80-bf6f-f6e7c10ded8e`. The run ended at a usage limit, not a completed architecture handoff. Its domain, identity and UI subagents left partial local drafts. Those files were preserved, reviewed and completed here. No missing sync, AI or date contract existed to recover.

## Owner decisions carried forward

- Keep the complete v1 scope and Bun Do's martial-arts bunny identity.
- Skip the separate vivo X300 Ultra/OnePlus 13 speech benchmark and accept feasibility as an unmeasured assumption.
- Use low or medium reasoning for subagents. Continue making architecture decisions while the owner is away.
- Provision new Azure resources in Bun Do's own resource group with Bicep. Foundry model availability is an owner-supplied assumption, not a request to reuse unrelated resources.
- Agents test through Android emulators on this Mac. Physical phones are unavailable.
- After the map is complete, implementation may begin. Run each slice's required fresh adversarial subagent review only after the whole slice is implemented and its planned verification has run. Follow the [completion-review workflow](agents/issue-tracker.md#completion-review); fix material issues and file concrete deferred bugs.

## What planning established

The implementation contract selects canonical-base/outbox replay, deterministic IDs and sequences, guarded version groups, bounded task graphs, Strong Cosmos revision transactions, stable snapshot recovery, durable AI jobs, recurrence identities and explicit date/account/restore semantics. The design contract selects a native Material direction and original small rabbit. Complete provider output schemas and literal recurrence identity vectors are checked in.

The build backlog includes every required feature and native blocking relationships. Azure integration and rendered UI are execution gates, not completed evidence. The old deployment-proof planning ticket is closed as outside the planning destination and points to its build replacement. The phone benchmark remains skipped.

## Verification performed during planning

- Recovered the prior session's final user instructions and checked the current GitHub dependency graph.
- Read all inherited domain/identity/design drafts and the original scope/review.
- Used low/medium subagents for recovery, environment evidence, AI/date decisions and bounded cross-contract adversarial checks.
- Corrected recurrence Base64 casing, seed-task identity, prediction commands, receipt lookup, snapshot storage switching, inherited graph checks and statistics retention discrepancies found during review.
- Parsed all strict-output schemas and checked required fields and closed object shapes.
- Independently recomputed both recurrence SHA-256/Base64url fixtures in Python.
- Checked local documentation links, Git whitespace and published dependency relationships before closing the map.

No application, tenant authentication, model invocation, Cosmos transaction or physical-phone test passed merely because the plan is written. Implementation tickets own that evidence.
